server:
  port: ${PORT:8081}
spring:
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:auth-service/.env[.properties]
  application:
    name: auth-service
  datasource:
    url: ${db_url:jdbc:mysql://mysql-17f92e09-quantitymeasurement-app.c.aivencloud.com:11428/defaultdb?useSSL=true&trustServerCertificate=true}
    username: ${db_username:avnadmin}
    password: ${db_password:AVNS_bC5Vu1vpm4P_ZSgRdse}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${google_client_id:28705293733-rp2kfvnhktufvu229nkfctnbr680htu1.apps.googleusercontent.com}
            client-secret: ${google_client_secret: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}
            scope: profile,email
eureka:
  client:
    service-url:
      defaultZone: ${eureka_url:https://eureka-service-qpx1.onrender.com/eureka/}
  instance:
    hostname: ${EUREKA_INSTANCE_HOSTNAME:localhost}
    prefer-ip-address: ${EUREKA_PREFER_IP_ADDRESS:true}
    non-secure-port-enabled: ${EUREKA_NON_SECURE_PORT_ENABLED:true}
    secure-port-enabled: ${EUREKA_SECURE_PORT_ENABLED:false}
    secure-port: ${EUREKA_SECURE_PORT:443}
jwt:
  secret: ${jwt_secret:9a4f2c8d3e7b1a5c6d8e0f1a2b3c4d5e6f7g8h9i0j1k2l3m4n5o6p7q8r9s0t1u2v3w4x5y6z}
  expiration: 86400000
